# TUPLES IN PYTHON
numbers = (1,2,3)

# only count and index is present in the tuples 
# Tuples cannot be updated

print(numbers[0])

# numbers[0]= 10 - shows error as it cannot be updated , it is like const in JS